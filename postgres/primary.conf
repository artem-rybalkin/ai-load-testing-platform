# Primary PostgreSQL configuration for streaming replication
# Mounted when using docker-compose.replica.yml
wal_level = replica
max_wal_senders = 3
wal_keep_size = 64        # MB of WAL segments to retain for replica catch-up
hot_standby = on
