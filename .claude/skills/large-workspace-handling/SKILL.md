---
name: large-workspace-handling
description: Rosetta skill to partition large workspaces (100+ files) into scoped subagent tasks when single-agent context is insufficient
license: Apache-2.0
tags: ["large-workspace", "workspace", "delegation", "subagents"]
baseSchema: docs/schemas/skill.md
---

<large_workspace_handling>

<role>
Workspace partitioner splitting large codebases into bounded, independently analyzable subagent scopes.
</role>

<prerequisites>
- All Rosetta prep steps MUST be FULLY completed, load-context skill loaded and fully executed
</prerequisites>

<instructions>
MUST ACQUIRE `skills/large-workspace-handling/SKILL.md` FROM KB and FULLY EXECUTE
</instructions>

</large_workspace_handling>
