# TODO.md — AI Load Testing Platform

Improvements, suggestions, and large future work items. Open items are listed first; everything under **Completed** below has already shipped and is kept for history/context, not for tracking active work.

## Open

All findings from the 2026-07-05 reverse-engineering, sensitive-data, and security-review passes are merged below into one severity-ordered list (**Critical → High → Medium → Low**), replacing the previous by-audit-pass/by-service grouping. Severity criteria: **Critical** = unauthenticated remote exploit or trivial cross-tenant breach; **High** = auth'd cross-tenant/privilege-escalation issues, live secrets/PII reaching a third-party LLM on every run, or correctness bugs that silently mask real failures/duplicate real-world side effects; **Medium** = bounded resource/quota/UX bugs with no direct data or security breach; **Low** = narrow-edge-case, latent, or cosmetic issues.

### Critical

*(all 5 fixed 2026-07-06 — see Completed section)*

### High

*(all 10 fixed 2026-07-06 — see Completed section)*

### Medium

*(all 10 fixed 2026-07-06 — see Completed section)*

### Low

*(all 13 fixed 2026-07-07 — see Completed section)*

### Checked and confirmed clean (audited, not skipped)

SQL injection (~310 query sites across all 8 services — every dynamic value goes through a `$N` parameterized placeholder; the only string-built SQL is hardcoded column names in dynamic `SET` clauses); command/argument injection (only two `spawn` sites touch untrusted data, both use the array form with no `shell: true` anywhere in the codebase); path traversal (`testId` is always server-generated `crypto.randomUUID()`, never request-derived; `csvFilename` is stripped and never touches disk); XSS (no `dangerouslySetInnerHTML`/`innerHTML`/markdown-to-HTML renderer anywhere in the UI); client-supplied `projectId`/`teamId` cross-tenant writes (every `/teams/:id/*`, `/orgs/:id/*`, webhook/log-source/schedule/preset/workspace route correctly cross-checks against the session's own membership); session fixation/token reuse (256-bit random tokens, sha256-hashed at rest); cookie flags (`sameSite: 'strict'`, `secure` correctly conditioned on `NODE_ENV==='production'`); decimal/octal/hex IPv4 SSRF-bypass encodings (normalized away by the `URL` constructor before the blocklist regex runs); `safeTestResponse()` strip of `envVars`/`testData`/`csvData`/`customScript` (applied consistently at every `POST /tests` response site); webhook HMAC secret (used only to sign outgoing payloads, never transmitted); per-team API-key creation (correctly admin-gated); client-side token storage (none — no localStorage/sessionStorage token use in the UI).

**Note on methodology**: one of the security-audit agents independently flagged that the untracked `.claude/rules/rosetta-bootstrap.md` file in this repo reads like a prompt-injection attempt (CRITICAL/BLOCKING language demanding a call to a nonexistent `get_context_instructions()` tool) and correctly disregarded it as untrusted content rather than complying. Not an application vulnerability (it's local repo config, not attacker-reachable input), but worth knowing it's there if you didn't intentionally author it.

### Follow-up analysis passes (remaining)

- [x] **1. Sensitive-data audit (`sensitive-data` skill)** — done, findings merged into the severity list above.
- [x] **2. Security-review pass (`security-review`)** — done (as a direct exploit-focused audit, not the diff-based `/security-review` command, which had nothing to review), findings merged into the severity list above.
- [x] **3. Regression tests for the findings above (`testing`/`testgen-flow`)** — done (2026-07-06): all 9 service groups complete. 40 of 42 findings have an automated `it.fails` regression test confirmed reproducing the bug; 1 (recorder-service's SSRF redirect gap) has no automated test but was instead confirmed via a live reproduction against the running stack (see below); 1 (results-service's multi-replica scheduler race) is genuinely untestable in a single Vitest process and was left undone. See "Regression test coverage added" below.
- [x] **4. Diff current implementation against existing spec docs (`requirements-use`)** — done (2026-07-06). Both docs turned out to be heavily self-corrected already (extensive "Correction note" annotations from a prior pass); verified a sample of their claims directly against current source rather than trusting the annotations at face value. Found: (a) `original-code-specs-auth-tenancy.md` §B.3 had a stale note explicitly concluding the cancel-endpoint cross-tenant gap was "correct behavior, not a bug" — wrong, and now corrected in the doc to match the Critical finding already in this list; (b) `reference-code-specs-project-isolation.md`'s U1 claimed `test_scripts.project_id` was always null for worker-inserted rows — verified fixed (`worker-backend/src/index.ts:70-71`'s `COALESCE`), doc updated to Resolved; (c) one new, previously-untracked bug surfaced this way — `GET /system/ai-status` unauthenticated cross-tenant leak — filed below under Low.
- [x] **5. E2E tests for the UI bugs (`aqa-flow`)** — done (2026-07-06): `e2e/error-handling-regressions.spec.ts`, 5 specs covering the compare-page crash, Stop/Set-baseline silent failures, webhook-creation silent failure, and the results list stuck on Loading forever. Each uses Playwright's `test.fail()` (the e2e equivalent of the Vitest `it.fails()` convention from item #3) — the desired-behavior assertion genuinely fails against current code, so the suite stays green while documenting the gap. Run against the real stack via a `mcr.microsoft.com/playwright:v1.60.0-jammy` container on `--network host` (local host lacked the Chromium runtime libs and no sudo was available to install them) — all 5 confirmed reproducing their bug live, plus the compare-page crash test additionally listens for the actual uncaught `pageerror` React throws with no `ErrorBoundary` in the tree.
- [x] **6. Formalize this pass into a maintained architecture doc (`code-analysis-flow`)** — done (2026-07-06). Folded the per-service write-up into a new "Service Internals — Deep Dive" section in `docs/ARCHITECTURE.md` (request lifecycles, auth flows, routing decision trees, retry semantics, and a cross-service patterns summary), trimmed to avoid duplicating the existing high-level tables. The scratch file it came from is no longer the source of truth.
- [ ] **7. 🚫 DO NOT TOUCH — Use `modernization-flow` for the two roadmap items below** — both are large, multi-step platform changes better served by a formal modernization plan than an ad-hoc investigation. Explicitly parked by the user (2026-07-06); do not pick this up proactively — wait for explicit go-ahead.

### Regression test coverage added (2026-07-06, follow-up item #3)

Every test below asserts the CORRECT/desired behavior and is wrapped in Vitest's `it.fails(...)` (not `.skip`/`.todo`) — the assertion inside genuinely fails against current code, which makes `it.fails` report as a passing test, so the suite stays green while permanently documenting the gap. The moment someone fixes the underlying bug without updating the test, `it.fails` flips to a hard failure ("unexpected pass") — that's the signal to remove the `.fails` wrapper and promote it to a normal test. One exception noted below (ui's stuck-loading test uses a never-resolving mock instead of a rejection, to avoid unhandled-rejection noise in the test runner).

- **packages/shared** — `packages/shared/src/__tests__/validateSsrfSafeUrl.test.ts`: 5 tests covering the IPv6-unhandled and trailing-dot SSRF bypasses. All confirmed reproducing.
- **api-service** — `services/api-service/src/__tests__/index.test.ts` (3 viewer-RBAC tests + 1 `parseDurationSeconds("500ms")` test) and `security.test.ts` (1 normal, non-`.fails` test documenting current `ALLOWED_ORIGIN=*` CORS behavior as a baseline contract). `parseDurationSeconds` was exported from `index.ts` (previously private) to make it directly testable — no other refactor. All confirmed reproducing.
- **ai-service** — `services/ai-service/src/__tests__/generator.test.ts` + `processor.test.ts`: 5 tests covering FLOW_PROMPT's unfenced PII, message-only 429 detection in both `generateScript` and `compareDescriptions`, the REGENERATE-then-retry requeue re-running comparison, and `compareDescriptions`'s missing `fenceUserContent()`. All confirmed reproducing.
- **analyser-service** — `services/analyser-service/src/__tests__/index.test.ts` + `aiInsights.test.ts`: 3 tests covering the unauthenticated `POST /analyse`, the missing internal AI-provider timeout (via `vi.useFakeTimers()`), and case-sensitive severity validation discarding a valid `"Critical"` insight. All confirmed reproducing (the timeout test validates the observable symptom — no bound enforced within a reasonable budget — since there's no existing timeout mechanism to assert an exact figure against).
- **recorder-service** — `services/recorder-service/src/__tests__/security-regression.test.ts`: 6 `it.fails` tests (auth headers forwarded to Gemini in cleartext, truncate-before-redact PII leak, 5xx correlation mis-attachment, racy `correlatorRateLimited` singleton, unanchored 429 substring match, generic Pino redact list) + 1 normal passing test (confirms `validateRecorderUrl` really does call the shared `validateSsrfSafeUrl`). Confirmed the IPv6/trailing-dot cases weren't duplicated (already covered by packages/shared's suite). The 7th finding — the redirect/DNS-rebinding gap — was left `it.skip` in the unit suite (genuinely needs a real browser against a live redirecting server) but was instead **confirmed live against the running dev stack** (2026-07-06): started a throwaway `python:3-alpine` container (`poc-redirect`) on the compose network serving a bare 302 to `http://127.0.0.1:6080/` (recorder-service's own noVNC port — a loopback address the SSRF blocklist explicitly rejects as a *direct* target), then called the real `POST /recordings/start` with `targetUrl: http://poc-redirect:8000/` (a hostname that passes validation cleanly). The recording captured a real step with `"url": "http://127.0.0.1:6080/"` — proof `page.goto()` silently followed the redirect straight past the SSRF guard, which only ever validated the pre-redirect URL. (Recorder-service's own AI-3 ignore-pattern feature even flagged `"suggestedIgnore": ["127.0.0.1"]` on the captured step — the system recognized the pattern as suspicious after the fact, but nothing blocked the fetch beforehand.) Throwaway container removed after the test; no lasting changes to the stack.
- **results-service** — 4 files: `api.test.ts` (cancel-cross-tenant missing `project_id` filter, `PUT /log-sources/:id` echoing `auth_header` via `RETURNING *`, unscoped schedule-enabled-state `SELECT`), `consumer.test.ts` (`fireWebhooks` skipping SSRF re-validation and following redirects — via a sentinel-webhook + `vi.waitFor` pattern since webhook firing is fire-and-forget), `ai-endpoints.test.ts` (HAR/chat-upload missing `redactPII()`, external-log content reaching AI prompts unredacted, `/ai/param-suggestions` missing `redactPII()`, `/ai/webhook-noise` missing the `project_id` filter, unescaped HAR filename in the prompt tag), and `quotas.test.ts` (non-atomic Gemini quota check-then-act). 11 of 12 findings confirmed reproducing; the scheduler leader-election finding was explicitly skipped — it needs two live `node-cron` processes racing, which a single Vitest worker can't represent.
- **worker-backend** — `services/worker-backend/src/__tests__/runner.test.ts` + `index.test.ts`: 3 tests covering the missing SIGKILL escalation on cancel (via fake timers past the grace period), the leaked run directory on a write failure, and the live-metrics data point lost when a poll lands mid-JSON-line. All confirmed reproducing, no source exports needed.
- **worker-client** — `services/worker-client/src/__tests__/runner.test.ts` + `index.test.ts`: 3 tests covering Web Vitals reporting `0` instead of `undefined` when `PerformanceObserver` never fires, a cancel silently dropped during the browser-pool-acquire race window, and a max-duration kill mid-Lighthouse-audit reporting `'completed'` instead of a failure. All confirmed reproducing.
- **ui** — `services/ui/__tests__/regression-http-error-handling.test.tsx` + `regression-results-loading.test.tsx`: 6 tests covering `cancelTest`/`setBaseline`/`clearBaseline`/`compareResults`/`createWebhook` all resolving silently on a non-2xx response instead of throwing, and the results list staying on "Loading…" forever. The stuck-loading test uses a never-resolving mock promise rather than a rejection, specifically to dodge a Vitest/jsdom quirk where a rejected promise's `unhandledRejection` reaches Vitest's process-level handler before jsdom's `window` event, making `preventDefault()` ineffective — the never-resolving mock proves the identical missing-`try/catch/finally` root cause without that noise. All confirmed reproducing.

### Roadmap (not severity-ranked)

- [ ] **Mobile application performance testing** — investigate approaches: Appium/WebDriverIO for native apps, cloud device farms (AWS Device Farm, BrowserStack), network throttling profiles, mobile-specific metrics (frame rate, battery, memory on device), Android/iOS instrumentation
- [ ] **Enterprise version with local LLM model** — investigate self-hosted/on-prem LLM (e.g. Ollama, vLLM, local Llama/Mistral/Qwen) as a drop-in replacement for Gemini for enterprise customers who can't send code/data to external APIs; needs a pluggable model-provider abstraction (`GEMINI_MODEL` → generic `AI_PROVIDER`/`AI_MODEL`), evaluation of script-generation quality vs. Gemini, GPU/resource sizing guidance, and config docs for air-gapped deployments

---

## Completed

*(shipped — kept for history/context)*

### Low bug-fix pass (2026-07-07) — all 13 Low findings, closing out the audit

Same convention as the prior passes: every fix flips a pre-existing `it.fails()`/`test.fail()` regression test back to normal. Full monorepo suite confirmed green modulo a handful of pre-existing wall-clock performance-budget tests (`correlator.test.ts`'s 50KB-body timing test, `parser.test.ts`'s 10k-line timing test, `api.test.ts`'s live-metrics timing test, `scheduler.test.ts`'s concurrent-fire test) that fail only under full-monorepo system load and pass cleanly in isolation — confirmed as pre-existing environmental flakes unrelated to any of these changes, not regressions.

- [x] **`GET /system/ai-status` unauthenticated cross-tenant leak** — removed from `app.ts`'s `publicPaths` exemption set (now requires normal session/API-key auth) and added the standard `project_id` filter to its query.
- [x] **`ALLOWED_ORIGIN` wildcard + credentials** — both api-service and results-service now set `credentials: allowedOrigin !== '*'` instead of always `true`, removing the risky wildcard+credentials combination when the env var is unset.
- [x] **Cross-tenant boolean oracle on schedule enable-state** — the pre-check `SELECT enabled FROM schedules` now carries the same project-scoping filter as the `UPDATE` right after it.
- [x] **HAR filename unescaped in prompt tag** — new `escapeXmlAttr()` helper applied to `att.filename` everywhere it's interpolated into the `<har_recording file="...">`/`<swagger_spec file="...">` wrappers.
- [x] **`compareDescriptions()` missing `fenceUserContent()`** — `storedDescription`/`newDescription` now fenced, matching the other three prompt builders in the same file.
- [x] **recorder-service Pino redact list** — now includes recorder-specific paths (`requestHeaders.authorization`, `x-api-key`, `x-auth-token`, `x-csrf-token`, `responseHeaders["set-cookie"]`) alongside the generic ones.
- [x] **`parseDurationSeconds()` ms-unit misparse** — regex alternation reordered to `(h|ms|m|s)` so `ms` is tried before the shorter `m` can shadow it; `"500ms"` now correctly parses to `1` second (rounded from 0.5) instead of `30000`.
- [x] **AI-insight severity case-sensitivity** — `parsed.severity` is now lowercased before the validity check and on the returned object, so a `"Critical"` response is accepted instead of discarded.
- [x] **`correlatorRateLimited` racy global singleton** — a concurrent session's success no longer resets another session's rate-limited flag to false; instead the flag self-clears after a 60s sticky window (`markRateLimited()`/`resetCorrelatorRateLimited()`) so `/health` doesn't report "rate_limited" forever once the underlying issue actually clears. (Note: this still doesn't give perfectly per-session attribution for `finishSession`'s result field under true concurrency — a documented, accepted trade-off for a Low-severity, mostly-`/health`-facing finding; a fully correct fix would thread the rate-limited status through `detectCorrelations()`'s return value instead of a module global.)
- [x] **429/quota unanchored substring match** — anchored to `/\b429\b/` in both `recorder-service/correlator.ts` and `analyser-service/aiInsights.ts` (ai-service's `generator.ts` got the same fix in the High-severity pass), so error text like "failed after 3429ms" no longer false-positives as a rate limit.
- [x] **worker-backend run-dir leak on write failure** — the file-write `Promise.all` and `validateScript` call are now both inside one try/catch that removes the run directory on any failure path, not just the old `validateScript`-only one. (Fixing this also surfaced and fixed an unrelated pre-existing test-pollution bug: the regression test for this finding queued an unconsumed `mockSpawn.mockReturnValueOnce()` that leaked into the next test's spawn queue, silently starving its `readAndPost` of a real process — masked for months by that same next test previously being an inverted `it.fails()`.)
- [x] **worker-backend live-metrics mid-line drop** — `readAndPost` now buffers an incomplete trailing line across polls the same way `makeLineBuffer` already does for stdout/stderr, instead of discarding both halves of a line split across two ticks.
- [x] **UI: `/results` list stuck on Loading forever** — `refresh()` now wraps the fetch in try/catch/finally, setting `loading` to `false` unconditionally and showing an error message instead of an infinite spinner. Verified against the live stack via the flipped Playwright spec.

### Medium bug-fix pass (2026-07-06) — all 10 Medium findings

Same convention as the Critical/High passes: every fix flips a pre-existing `it.fails()`/`test.fail()` regression test back to normal. Full monorepo suite confirmed at 93 files / 1917 passing / 11 expected-fail (the remaining Low findings) after this batch, zero regressions.

- [x] **Gemini daily quota non-atomic check-then-act** — new `checkAndIncrementGeminiUsage()` (`services/results-service/src/quotas.ts`) collapses the check and increment into one `INSERT ... ON CONFLICT (team_id, usage_date) DO UPDATE SET call_count = call_count + 1 WHERE call_count < $limit RETURNING call_count`; no returned row means quota exhausted. All 9 `/ai/*` handlers in `routes/ai.ts` that previously called `checkGeminiQuota` then `incrementGeminiUsage` around the AI call now make one atomic call before it.
- [x] **`PUT`/`POST /log-sources/:id` echoing `auth_header` via `RETURNING *`** — both now `RETURNING` an explicit column list matching `GET /log-sources`'s own `SELECT`, omitting `auth_header`.
- [x] **No internal timeout on analyser-service's AI-provider call** — `generateAiInsights()` now races the retry loop against a 10s `setTimeout` guard (not `AbortSignal.timeout`, which uses timers Vitest's fake-timers can't intercept), matching results-service's 12s outer bound closely enough that a hung provider no longer runs ~4x longer than the caller already gave up waiting for.
- [x] **recorder-service correlation mis-attachment on 5xx** — `detectCorrelations`/`applyCorrelations` now build and thread an unfiltered→filtered step-index map (`buildIndexMap`) instead of assuming the two arrays stay 1:1 once a 5xx-containing request is filtered out of the step list.
- [x] **worker-backend cancel has no SIGKILL escalation** — the cancel-fanout consumer now starts a 30s grace-period timer after `SIGTERM` (mirroring the existing max-duration watchdog's own escalation), sending `SIGKILL` only if the process is still tracked as running when it fires.
- [x] **worker-client: cancel during browser-pool acquisition dropped + Lighthouse-phase kill reported as "completed" (2 findings)** — the cancel handler now records the cancellation unconditionally (not gated on `runningBrowsers` already being populated), so it's honored as soon as `runClientTest` returns; a `killTimerFired` flag set by the max-duration timer is now checked after Lighthouse's catch block, re-throwing instead of falling through to a false "completed" status.
- [x] **UI: compare-page crash + webhook silent failure (2 findings)** — `compareResults()`/`createWebhook()` now check `res.ok` and throw, matching the convention already applied to `cancelTest`/`setBaseline`/`clearBaseline`; both call sites already had error handling in place (a `.catch()` on the compare page, a try/catch in `handleAdd`), so no component changes were needed beyond the API-layer fix. Verified against the live stack via the flipped Playwright specs.
- [x] **ai-service: 429 detection gap + `compareDescriptions` retry-redo (2 findings)** — rate-limit detection now uses a word-boundary-anchored `/\b429\b/` + `'quota'` check instead of `.status === 429` alone, deliberately more precise than the unanchored `msg.includes('429')` pattern the sibling services use (still tracked as its own Low finding). The REGENERATE-then-retry requeue now publishes the current mutated `test` object (with `cachedScript`/`cachedScriptDescription` cleared) instead of the original message buffer, so a retry no longer redoes the comparison from scratch.

### High bug-fix pass, round 2 (2026-07-06) — the remaining 10 High findings

Same convention as round 1 below: every fix flips a pre-existing `it.fails()`/`test.fail()` regression test back to normal, verified failing-before/passing-after. Full monorepo suite (`npx vitest run`) confirmed at 93 files / 1905 passing / 23 expected-fail (the remaining Medium/Low findings) after this batch, zero regressions.

- [x] **`FLOW_PROMPT` sends step data to Gemini unredacted** — `services/ai-service/src/generator.ts`'s `stepDefs` builder now wraps `s.url`, `s.body`, and `JSON.stringify(s.headers)` in `fenceUserContent()` each, matching the convention `BACKEND_PROMPT`/`CLIENT_PROMPT` already used for `test.description`. The `{{varName}}` placeholder-substitution logic (which reads the same fields before fencing) is unaffected.
- [x] **Chat/HAR-upload + external-log-source + `/ai/param-suggestions` PII/fencing gaps (3 findings)** — `services/results-service/src/routes/helpers.ts`'s HAR summary, `externalMetrics.ts`'s fetched log text, and `routes/ai.ts`'s param-suggestions step data all now pass through `redactPII()` before reaching a prompt; the external-log text and the `/diagnose` prompt that consumes it are also wrapped in `fenceUserContent()` (it was previously uncontrolled third-party content with zero prompt-injection mitigation, on top of the missing redaction).
- [x] **`POST /ai/webhook-noise` missing `project_id` filter** — `routes/ai.ts`'s query now applies `AND ($1::uuid IS NULL OR project_id = $1::uuid)`, matching every sibling AI-assist query in the same file.
- [x] **recorder-service: auth headers forwarded to Gemini in cleartext + PII-truncation ordering (2 findings)** — `correlator.ts`'s `buildSummary()` now redacts auth header *values* (keeping header names, since correlation needs to know a header exists) and additionally scrubs the same token values out of response bodies (a token from step N's response can reappear verbatim in step N+1's request header). Truncation now runs with a 300-char safety margin *after* `redactPII()`, so a PII value straddling the old 500/2000-char cutoff is fully visible to the regex before the final truncation — avoided redacting the full unbounded body directly since `EMAIL_RE` is O(n²) and blew a performance budget test on large bodies.
- [x] **Scheduler leader election** — `services/results-service/src/scheduler.ts`'s `triggerSchedule` now wraps each fire attempt in `pg_try_advisory_xact_lock(hashtext('${schedule.id}:${currentUTCMinute}'))`; only the replica that acquires the lock proceeds to `POST /tests` and updates `last_run_at` in the same transaction, all others silently skip. No new migration needed (the lock is transient, keyed per schedule per minute). New test in `scheduler.test.ts` fires the same schedule's callback twice concurrently against a real Postgres pool and asserts exactly one `POST /tests` call.
- [x] **worker-client: LCP/FCP/CLS/TTFB report `0` instead of `undefined`** — `runner.ts`'s final metrics object now coalesces all four to `undefined` when their `avgNum()` result is `0`, matching the existing `jsErrors`/`longTaskCount` pattern. `packages/shared/src/contracts.ts`'s `ClientMetricsSchema` had `lcp`/`cls`/`ttfb`/`fcp` made `.optional()` to match the now-possibly-absent fields.
- [x] **UI: Stop / Set-baseline / Clear-baseline silent failures** — `lib/api/tests.ts`'s `cancelTest`/`setBaseline`/`clearBaseline` now check `res.ok` and throw with the server's error message; the result detail page (`app/results/testId/page.tsx`) gained an `actionError` state + catch blocks on `handleCancel`/`handleBaseline`, rendering the error next to the action buttons (there was previously no error UI at all for these two actions). Verified against the live stack via the flipped Playwright specs in `e2e/error-handling-regressions.spec.ts`.

### Critical/High bug-fix pass, round 1 (2026-07-06) — 5 Critical + 4 bonus High findings

Every fix here flips a pre-existing `it.fails()`/`test.fail()` regression test (added 2026-07-06, see the "Regression test coverage added" note further down) back to a normal passing test — each one was verified failing-for-the-right-reason before the fix and passing-for-the-right-reason after.

- [x] **SSRF validator: IPv6 unhandled + trailing-dot bypass (2 Critical)** — `packages/shared/src/index.ts`'s `validateSsrfSafeUrl` now checks bracketed IPv6 literals (loopback `::1`, unique-local `fc00::/7`, link-local `fe80::/10`, IPv4-mapped `::ffff:x.x.x.x` re-checked against the IPv4 blocklist) and strips a trailing dot before matching hostnames. `packages/shared/src/__tests__/validateSsrfSafeUrl.test.ts`'s 5 tests all pass normally now.
- [x] **DNS-rebinding SSRF + redirect-following, platform-wide (1 Critical + 1 High)** — new exported `fetchSsrfSafe(url, init, maxRedirects)` in `packages/shared/src/index.ts`: re-validates the hostname on every call (not just once at creation time), resolves DNS and checks the resolved IP too (dynamically imports `dns/promises`, following the same pattern `aiProvider.ts` uses for other Node-only deps, so this package stays browser-safe for the UI), and manually follows redirects re-validating each hop. Applied to `results-service`'s `fireWebhooks()` (`consumer.ts`), `fetchExternalMetrics()` (`externalMetrics.ts`), and the Swagger/spec-import fetches (`routes/helpers.ts`). recorder-service's equivalent gap (Puppeteer navigation, not `fetch`) closed separately via `page.setRequestInterception(true)` + a `createSsrfInterceptHandler()` that validates every request/redirect hop against `validateSsrfSafeUrl`, aborting unsafe ones (`recorder.ts`). All confirmed via their respective regression tests.
- [x] **`analyser-service`'s `POST /analyse` unauthenticated + prod-exposed (Critical)** — added an unconditional `onRequest` hook requiring `X-Internal-Key` to match `INTERNAL_API_KEY` (deliberately *not* the rest of the codebase's "no-op when the key is unset" convention — this endpoint triggers a real, costed LLM call, so there's no safe "auth disabled" mode for it). `results-service/src/consumer.ts` now sends that header via `internalHeaders()`. `docker-compose.prod.yml` gained the missing `analyser-service` override (`ports: !reset []`), matching its siblings, as defense-in-depth alongside the app-level check.
- [x] **Cross-tenant test cancellation (Critical)** — `services/results-service/src/routes/results.ts`'s cancel handler now filters `AND (project_id IS NULL OR project_id = $2::uuid)`; unlike the read-side `($N::uuid IS NULL OR ...)` convention, a *missing* caller projectId is NOT treated as unrestricted here (this route has no session of its own) — only a row with no owner stays universally cancellable. `services/api-service/src/index.ts`'s cancel handler now forwards its own `request.projectId` in the call body.
- [x] **`viewer` role not enforced on api-service mutations, incl. `customScript` code-exec reachability (2 High)** — api-service's `onRequest` hook now returns 403 for `role === 'viewer'` on any mutating method (POST/PUT/PATCH/DELETE), mirroring results-service's existing rule. Since `POST /tests` itself is now blocked for viewers, the `customScript` code-execution reachability closes as a direct consequence — no separate fix needed.

### Test coverage gap audit (2026-07-05) — full findings in `docs/TEST-COVERAGE-GAPS.md`

- [x] **`ai-service/src/index.ts` had zero direct test coverage** — `index.ts` now exports `app`/`startConsumer`/the queue-name constants and guards its module-scope `start()` call with `if (!process.env.VITEST)`, matching the `buildApp()` pattern already used elsewhere. `index.test.ts` was rewritten to drive the real module against an EventEmitter-mocked `amqplib` connection/channel instead of a 250-line hand-copied decision tree that could silently drift from the real routing logic. 9 tests, all real-module.
- [x] **Neither worker's (nor api-service's) AMQP reconnect wiring was tested** — `worker-backend`, `worker-client`, and `api-service/src/queue.ts` all had the same gap: connection/channel `close`/`error` handlers and reconnect-dedupe were never exercised by a test. All three fixed the same way (export `app`/`start`, guard module-scope startup + `SIGTERM`/`SIGINT` registration with the `VITEST` check, test real event handlers via EventEmitter-mocked amqplib). Found and fixed along the way: `worker-client/src/index.ts` had **no `VITEST` guard at all** — every test run was silently trying to bind a real port and connect real AMQP, which explains the recurring `EADDRINUSE` warnings seen throughout earlier work this week. Also replaced `worker-backend`'s redundant `handleRetryFn`/`validateScriptFn` hand-copies (already covered for real in `runner.test.ts`) with real tests of the Stop-button cancel branch and the cancel-queue consumer. 405 tests passing across the four services (was ~35 fewer, several of which were dead-weight duplicates).
- [x] **UI Settings page had zero test coverage** — `SettingsPage.test.tsx` added, 15 tests covering the non-admin branch (access-required message, setting never fetched) and the admin branch (loading/error states, all three window options rendering, Save disabled until a different option is picked, save success/in-flight/error states, error clearing on a subsequent successful save).
- [x] **E2E only ran on post-merge pushes to `main`, never on PRs** — the `e2e` job's `if: github.ref == 'refs/heads/main'` condition is never true for a `pull_request` event, so a UI regression only surfaced after merge. Now also runs when `github.event_name == 'pull_request'`.
- [x] **No contract/schema validation between services** — every AMQP consumer parsed untrusted queue messages with a raw `JSON.parse(...) as Type` cast (compile-time-only, nothing enforced at runtime). Added `zod` to `packages/shared` with a schema per message shape crossing a queue boundary (`TestRequestSchema`, `EnrichedTestRequestSchema`, `TestResultSchema`, `CancelMessageSchema`), each paired with a compile-time two-way assignability check against the existing hand-written interface (so drift fails `tsc` without migrating every import site to a zod-derived type). All 5 consume sites now validate through their schema; 25 contract tests cover minimal/fully-populated examples and rejection of missing/mistyped fields. Found and fixed along the way: `results-service/src/consumer.ts` and both workers' cancel-fanout consumers had **no malformed-message guard at all** — a bad message threw uncaught inside the async `consume` callback and was never acked.
- [x] **`results-service/src/consumer.ts`'s `startConsumer` had no real-module coverage** — same class of gap as the worker/api-service fix above, just not in that pass's original scope, plus its retry/DLQ logic was still a hand-reimplementation. Closed by giving `startConsumer`/`scheduleReconnect` an optional `{ pool?: Pool }` override (defaults to the module's own pool — production behavior unchanged) so tests can point it at the file's existing Testcontainers database without mocking `db.ts`. New `startConsumer.test.ts` (16 tests) covers connection lifecycle, the malformed-message/schema-validation guards (verified against the real DB — zero rows written), a valid message persisting via the real `handleResult`, and the full retry/DLQ-exhaustion matrix.

### Performance review (2026-07-04) — quick wins

- [x] **Missing DB indexes on hot paths** — migration #16 (`perf_indexes`) adds `idx_team_members_user_id`/`idx_org_members_user_id` (login/switch-team/`/auth/me` previously forced a full scan), `idx_test_results_project_created`/`idx_test_results_workspace_created` composite indexes for `GET /results`'s filter+sort (superseding and dropping the old single-column `idx_test_results_project_id`), and `idx_test_results_url_type_status_created` for `consumer.ts`'s baseline-diff lookup (superseding and dropping the old 3-column `idx_test_results_url_type_status`, which covered the filter but forced a sort over every matching row).
- [x] **`GET /orgs/:id` N+1 query** — added `getTeamQuotasAndUsageBatch()` (`quotas.ts`) doing 4 batched `WHERE team_id = ANY($1::uuid[])` queries total regardless of team count, replacing the `.map(async ...)` loop in `routes/teams.ts` that issued 4×N queries for N teams.
- [x] **`GET /results` over-fetches with `SELECT *`** — now selects an explicit column list (id/test_id/type/target_url/status/perf_status/metrics/is_baseline/created_at/completed_at/started_at/duration_seconds/status_message/script_description) instead of `r.*`, excluding the large `script`/`execution_log`/`analysis`/`steps`/`test_data` columns that no list-view consumer (`app/page.tsx`, `app/results/page.tsx`) actually reads.
- [x] **UI: duplicate active-tests polling** — `activeTests` moved into the existing `HealthContext` (one shared fetch + one debounced WS subscription); `Sidebar`, `ActiveTests`, and the home page now all read `useHealth().activeTests` instead of each independently polling `getActiveTests()` and subscribing to the same WS events.
- [x] **`AuthContext` not memoized** — `logout`/`switchTeam` wrapped in `useCallback`, provider `value` wrapped in `useMemo`, so `useAuth()` consumers no longer re-render on every unrelated `AuthProvider` render.
- [x] **ai-service blocks on non-essential notification POST** — the "Generating test script…"/"Script ready…" status POSTs in `processor.ts` are now fire-and-forget (`void postMessage(...)`, which already swallows its own errors) instead of `await`ed ahead of/after script generation.
- [x] **`sessions`/`audit_log` tables have no retention/purge** — `runStaleCleanup` now also deletes expired/revoked `sessions` rows unconditionally (no grace period — an expired/revoked session has zero future purpose) and `audit_log` rows older than `AUDIT_LOG_RETENTION_DAYS` (default 180, mirroring the `TEST_RESULTS_RETENTION_DAYS` pattern) on every cleanup cycle.
- [x] **Worker SIGTERM doesn't drain in-flight tests** — `shutdown()` in both `worker-backend` and `worker-client` now cancels the queue consumer first (`channel.cancel(consumerTag)` — stops new deliveries, doesn't affect messages already in flight), then awaits the new shared `drainInFlight()` (`@alt/shared`) polling an `inFlightMessages` counter down to 0 — bounded by `MAX_TEST_DURATION_MS` (+ `GRACE_PERIOD_MS` for worker-backend's k6 SIGTERM→SIGKILL) plus a 10s buffer for post-exit bookkeeping (DB write/result-queue send/ack) — before closing the AMQP channel/connection and exiting. The in-flight counter tracks the full message-handler lifetime (not just k6-process/browser lifetime), so the channel can't close mid-ack. `docs/how-to/production.md`'s claim about graceful-SIGTERM-drain-during-scale-down is now actually true rather than aspirational.
- [x] **Per-log-line HTTP POST had no batching** — `worker-backend`'s `addLogLine`/`worker-client`'s `addLog` fired one unawaited POST per stdout/stderr line (up to 5000/test). Added `createBatcher()` (`@alt/shared`, flushes every 300ms or every 50 items, whichever first — also unit-tested directly) and wired it into both workers' log-line accumulation, with an explicit `flush()` on k6 close/error and in `runClientTest`'s `finally` so trailing lines aren't stranded. `POST /results/:testId/log-line` now accepts `{ lines: [{level,line}, ...] }` instead of a single `{level,line}`, broadcasting one `test:log` WS event per line so the UI's real-time streaming is unaffected.
- [x] **Puppeteer launched a fresh browser per test — no pooling (2026-07-05)** — new `worker-client/src/browserPool.ts`: `createBrowserPool()` reuses idle browser *processes* across sequential test runs on the same worker instead of paying Chrome's ~1-3s launch cost on every dequeue, sized to `WORKER_CONCURRENCY` (no benefit pooling more than the max concurrent tests). Pools whole browsers, not shared contexts — a client-side test's Lighthouse audit needs a browser's CDP port to itself (it enumerates and closes every open page), so each pooled browser stays checked out to exactly one test at a time, same exclusivity as before. `release()` (called unconditionally in `runClientTest`'s `finally`, whether the test succeeded, failed, or was cancelled) resets a still-connected browser (closes its pages, removes the `targetcreated` listener `runClientTest` re-attaches on every call) and returns it to the idle pool; a disconnected browser (killed by cancel, the max-duration timer, or a real crash) is simply dropped. 8 new tests for the pool itself plus 2 updated + 2 new tests in `runner.test.ts` covering reuse across two calls and discarding a disconnected browser.
- [x] **worker-backend: unbounded stdout/stderr buffers + synchronous regex parsing (2026-07-05)** — `stdoutChunks`/`stderrChunks` accumulated every chunk for a k6 process's *entire* run, then got regex-parsed in one synchronous pass in the `close` handler — unbounded memory for a long/verbose test, and a parse cost proportional to total output instead of to the (fixed, few-KB) end-of-test summary block those regexes actually look for. New `makeTailBuffer(maxBytes)` in `runner.ts` keeps only the trailing N bytes of everything pushed (64KB for `runK6Test`'s stdout/stderr, 8KB for `validateScript`'s stderr) — the summary is always the last thing k6 writes, so a small bounded tail is enough, and both the memory footprint and the final parse cost are now constant regardless of test duration/verbosity. 4 new tests for the buffer itself.
- [x] **UI memoization gaps + results-service AI-provider double-fetch (2026-07-05)** — `LiveCard`'s sparkline path (`sparklinePath()`) recomputed on every 1s elapsed-timer tick even though it only depends on `points`; wrapped in `useMemo([points])`. Home page's stat-band aggregates (`passRate`/`avgP95`/`avgRps`/tests-today — several `.filter()`/`.reduce()` passes over `recent`) recomputed on every unrelated re-render (form field edits, toggles); wrapped in a single `useMemo([recent])`. `ExecutionLogPanel`'s `allEntries`/`visible`/`rawText`/`counts` (parsing up to 5000 stored log lines, filtering, joining, and counting) recomputed on every render regardless of whether the log data changed; each wrapped in its own `useMemo` keyed on the actual inputs. Separately, every results-service `/ai/*` route (9 of them) called `isAiConfigured(pool)` then `aiGenerateText(pool, prompt)` — two identical `SELECT ... app_settings WHERE key = 'ai_provider'` queries per request. New `getAiCapability(pool)` in `routes/helpers.ts` fetches the setting once and returns both the configured flag and a bound `generateText`, replacing `isAiConfigured`/`aiGenerateText` at all 9 call sites in `ai.ts` plus the 1 in `results.ts` (AI-8 executive summary).
- [x] **RealtimeChart.tsx dataviz fixes, batch 1 (2026-07-05)** — dual-axis chart split: the "VUs & Error Rate" panel plotted VUs (a raw count) and error rate (%) on two y-axes, the #1 flagged dataviz anti-pattern; VUs now gets its own single-axis "Virtual Users" panel, and the error-rate panel is purely percentage-based, one axis. Gridlines/tick colors (`CartesianGrid stroke="#f2ede2"`, `TICK.fill = '#6b6557'`) were light-mode-only hex literals that never adapted to dark mode, unlike the `Tooltip` in the same file — both now use `var(--border-2)`/`var(--tx-3)`. `StepLegend` rendered identically 3x (once under each per-step panel); now renders once, shared across all three. See batch 2 for the remaining dark-mode/CVD/contrast/table-view findings, which needed actual color-value decisions via `validate_palette.js`.
- [x] **RealtimeChart.tsx dataviz fixes, batch 2 (2026-07-05)** — dark-mode line-color calibration: validated via `validate_palette.js` against the app's actual chart surface (dark `#1c180f`), 3 colors FAILed the OKLCH lightness band [0.48, 0.67] (`#ff5a2c` orange, `#ca8a04` amber both over the ceiling; `#991b1b` server-5xx under the floor). Added dedicated `--chart-orange`/`--chart-amber`/`--chart-red-dark` CSS custom properties (light = original hex, dark = a retuned step at the same hue/chroma, nudged into the dark band) rather than reusing the existing `--*-fg` badge tokens, which are tuned for text contrast — too bright for a thin chart stroke; all now PASS in both modes. CVD secondary encoding: the worst adjacent `STEP_COLORS` pair (orange↔green, ΔE 10, floor band) had no non-color distinguishing signal; step lines and their legend swatches now alternate solid/dashed by index, so any two adjacent steps differ in pattern as well as hue. Amber contrast relief + table-view twin: added a table-view toggle (`📋 Table view` / `📈 Chart view`) rendering an accessible HTML table of the same data (aggregate or per-step columns) — the skill's own rule is that a contrast WARN (amber sits at 2.94:1, below the 3:1 floor) obligates visible labels or a table view, not dismissable via the tooltip alone; this closes that and the separately-tracked table-view gap in one implementation. 5 new tests for the table view; all existing RealtimeChart tests still pass (30 total). Remaining: tooltip ordering (low priority, cosmetic).
- [x] **RealtimeChart.tsx dataviz fixes, batch 3 — tooltip ordering (2026-07-05)** — Recharts' default tooltip rendered `name: value` with a filled box swatch; the dataviz skill's interaction spec wants value leading (the reader has the series, wants the number), name secondary, and a line-key (a short stroke) instead of a box, since a filled box is data-weight ink doing a label's job at tooltip density. Replaced all 4 `formatter`/`contentStyle` tooltips with a shared `ChartTooltip` custom `content` renderer matching that spec, reusing the same solid/dashed swatch logic as the legends. 6 new direct tests (`ChartTooltip` exported and tested standalone, since the existing test file mocks all of recharts to null renderers — the old `formatter`-based tooltips were never actually exercised by a test either). Closes the last open item from the `/dataviz` skill audit; only the two "investigate" items (mobile testing, local-LLM enterprise version) remain in Open.

### Manual Testing

- [x] Manually test log integration — verified live 2026-06-23: ELOG-01 (view log after completion, severity filter), ELOG-02 (real-time streaming — superseded by a live run during testing), ELOG-03 (`POST /results/:testId/log-line` correctly 401s without/with-wrong `X-Internal-Key`, succeeds with the correct one). ELOG-04 (5000-line/100KB cap) not separately re-verified — lower priority, mechanism unchanged since implementation.
- [x] Manually test script recording — verified live 2026-06-23 via claude-in-chrome: real CDP recording session (6 requests captured), step import with static-asset filtering (5 of 6 imported), AI-2 step naming ("Search on Google", "Load reCAPTCHA anchor"), AI-3 ignore-pattern suggestion (`www.google.com`), AI-12 think-time hints on all imported steps.
- [x] Manually test correlation
- [x] Manually test client-side testing — already covered by the 2026-06-21/06-22 full test run (Section 9), where 3 of the 9 found bugs were in this exact area, all since fixed and re-verified live.

### AI Enhancement Roadmap

#### 🔴 High impact — implement now
- [x] **AI-1: Smart threshold suggestions** — `✨ Suggest` button in SLO panel; calls `/results/suggest-thresholds`; fills p95/avg/errorRate from run history via Gemini
- [x] **AI-2: Auto-name flow steps** — Automatic after recording stops; Gemini renames `Step N: POST /path` → `Authenticate — get bearer token`
- [x] **AI-3: Ignore-list suggestions after recording** — Blue banner after recording stop with detected analytics/CDN domains + one-click "Add all to Ignore list"
- [x] **AI-4: Error root-cause classification** — `✨ Diagnose with AI` button in Error Breakdown card; shows likely cause + next step per error category

#### 🟡 Medium impact — implement next
- [x] **AI-5: Cron natural-language assistant** — Text input + `✨ Convert` button under cron field in Schedules; `POST /ai/cron` → Gemini → fills cron expression + preview
- [x] **AI-6: Playwright → k6 translator** — `✨ Translate Playwright` file-upload button in Custom Script mode; `POST /ai/translate` on results-service → Gemini → drops k6 script into textarea. (Originally lived in ai-service as a raw Gemini SDK call the UI's `/api` proxy could never actually reach — moved to results-service 2026-06-24, following the same `generateAIText`/quota/rate-limit pattern as every other `/ai/*` endpoint; see "High Priority (Tech Debt)" below.)
- [x] **AI-7: Regression narrative across runs** — `✨ Summarise trend` button on Trend chart card; `POST /ai/trend-narrative` → Gemini → 2-sentence summary shown below chart
- [x] **AI-8: PDF executive summary** — Auto-generated 3-sentence executive paragraph at top of every PDF report; non-fatal if Gemini unavailable
- [x] **AI-9: Optimal VU/duration recommendation** — `✨ Suggest settings` link below URL field; `GET /results/suggest-settings` → Gemini → fills VUs, duration, profile in Advanced settings
- [x] **AI-10: Parameterisation suggestions in FlowBuilder** — `✨ Suggest columns` button in Test data section; `POST /ai/param-suggestions` → Gemini → pre-fills column headers

#### 🟢 Low impact — implement later
- [x] **AI-11: Alert noise prediction** — Noise check fires when toggling webhook events; `POST /ai/webhook-noise` → Gemini analyses run history → amber warning or confirmation shown inline
- [x] **AI-12: Think-time suggestion from recording** — `computeThinkTimes()` derives ms gaps from request timestamps; meaningful pauses (>500ms) shown as `⏱ 1.2s` hints next to each step in FlowBuilder
- [x] **AI-13: Step deduplication after recording** — `detectDuplicateSteps()` identifies same-endpoint steps with varying query params; yellow banner in FlowBuilder with merge suggestion
- [x] **AI-14: Preset name + tag auto-suggestion** — On "Save preset", `POST /ai/preset-name` → Gemini → auto-fills name + shows tag chips; non-fatal fallback to description/URL

#### 🆕 New ideas — not yet prioritized
- [x] **AI-15: Fallback to another provider (OpenAI/Claude API)** — done; see [docs/AI-FEATURES.md → Model configuration & multi-provider fallback](docs/AI-FEATURES.md#model-configuration--multi-provider-fallback) for the full provider abstraction, per-team override, and UI details.
- [x] **AI-16: Better prompt engineering** — BACKEND_PROMPT: added JSON-API pattern, auth via `__ENV`, body assertion requirement, explicit threshold defaults (p95<1000, rate<0.01), two concrete `http.post`/`http.get` code examples. CLIENT_PROMPT: expanded with PerformanceObserver Web Vitals injection pattern (LCP/CLS/INP), TTFB via NavigationTiming, FCP via paint timing, interaction pattern (waitForSelector/scroll), multi-session structure. FLOW_PROMPT: added concrete 2-step login→fetch extraction example in structure section showing defensive `.json()` guard and `exec.vu.abort`.
- [x] **AI-17: Better prompt engineering for analyser-service** — `aiInsights.ts`: added INP/TBT to `ClientPromptMetrics` and `formatMetrics` (with good/needs-improvement/poor thresholds inline); added per-test-type root cause focus hints (backend: pool exhaustion/GC/keep-alive; flow: per-step gaps/auth expiry; browser: render-blocking/LCP/CLS/INP); added one-shot JSON example output at the end of the prompt to anchor output format.
- [x] **Live metric graph shows errors not present in k6 logs** — investigated: not a metrics bug. The live chart's `errorRate` (`worker-backend/src/parser.ts` `aggregateWindow`) reads k6's built-in `http_req_failed` metric directly, which is accurate. The execution log (`worker-backend/src/runner.ts` `addLogLine`) is a literal capture of k6's stdout/stderr — and k6 does not print anything when an individual request fails or a `check()` fails; it only emits `ERRO`/`WARN` text for k6-level events (script exceptions, threshold violations at run end). So real failures move the error-rate line correctly while leaving nothing in the log to correlate them with. Fixed the observability gap (not the metric) by having `BACKEND_PROMPT` (`ai-service/src/generator.ts`) instruct generated scripts to `console.error` the status + URL whenever a request fails (`res.status === 0 || res.status >= 400`), right after the existing `check()` call — so the execution log now explains what the chart is showing.
- [x] **Configurable live metric graph granularity** — admin-configurable via a new **Settings** page (`/settings`, admin-only), not per-test: 10s / 30s / 1min, default 30s (changed from the original 10s shortly after launch). Global setting stored in `app_settings` (key `live_metric_window_sec`, mirrors the existing `ai_provider` pattern) via `GET/PUT /system/live-metric-window` in results-service (`settings.ts`, `routes/system.ts`); `GET` is auth-exempt (`app.ts`) so `worker-backend` can read it without a session, the same way `ai-service` reads `/system/ai-provider`. `worker-backend` fetches it once per test at dequeue time with a 30s in-memory cache (mirrors `ai-service/generator.ts`'s `getProviderSetting`), so **a setting change only affects tests that start after the change — never one already running**. `parser.ts`'s `aggregateWindow(lines, windowSec)` and `runner.ts`'s live-metric polling now take the window as a parameter (backward-compatible default of the old fixed 2s) instead of the hardcoded `LIVE_WINDOW_SEC` constant. `RealtimeChart.tsx`'s window label is derived from consecutive live-point timestamps (snapped to the nearest known bucket) rather than plumbed through, since `test_results` has no `options` column to persist it on. Scope: `backend`/`flow` (k6) tests only — `client-side` (Puppeteer) has no equivalent live-metrics loop.

### High Priority (Tech Debt)

- [x] **Security/correctness fixes from a senior fullstack+PM readiness review (2026-06-24)** — found via independent audit (not the manual test suite), all fixed and tested same day:
  - **`log_sources` SSRF + cross-tenant credential leak** — `fetchExternalMetrics` (the server-side fetch behind `GET /results/:testId/diagnose`'s external-observability lookup) had zero `project_id` scoping and zero SSRF validation on `metrics_endpoint_template`, despite the sibling `webhooks` feature having both. Fixed: `validateSsrfSafeUrl` now applied to `urlTemplate`/`metricsEndpointTemplate` on `POST`/`PUT /log-sources`; `fetchExternalMetrics` scoped by `project_id`; `GET /log-sources` no longer returns `auth_header` to any team member (it held a raw bearer/API token).
  - **Raw internal errors leaking on all 9 AI endpoints** — every `/ai/*`, `/chat/parse`, `suggest-settings`, `suggest-thresholds`, `diagnose` route returned `(err as Error).message` straight to the client on any unhandled exception. Fixed with a shared `sendInternalError` helper (logs server-side, returns a fixed generic message) — same fix already applied once for `DELETE /scripts/:id`, now generalized.
  - **`/translate` (AI-6) was unreachable from the browser** — the UI called `${API_URL}/translate` (api-service, which never registered the route) instead of `${RESULTS_URL}` like every other AI-assist call; the route only existed in ai-service, with no proxy path to it. Also bypassed the pluggable AI provider abstraction (raw Gemini SDK call). Moved to `results-service` as `POST /ai/translate`, following the standard `generateAIText`/quota/rate-limit pattern; removed the orphaned route + its standalone test file from ai-service; fixed `translatePlaywright()` in `lib/api.ts`.
  - **Investigated, no action needed**: `analyzer.ts` in `results-service` and `analyser-service` were flagged as "byte-for-byte duplicated business logic" — turned out both are already 2-line re-exports of `@alt/shared`'s single `analyzeResult` (deduplicated in an earlier phase); false positive, left as-is.
- [x] **Rate limiting (Redis — priority #1)** — Wire up Redis; add `@fastify/rate-limit` to api-service and results-service to protect Gemini API and REST endpoints
- [x] **Recorder-service test coverage** — recorder.test.ts (262 lines), correlator.test.ts (302 lines), api.test.ts (385 lines)
- [x] **Playwright → k6 converter (AI-6)** — `POST /ai/translate` in results-service (Gemini-based, via the shared AI provider abstraction) + "✨ Translate Playwright" file upload in the home page's Custom Script mode (`translatePlaywright()` in `lib/api.ts`)
- [x] **Lint cleanup — remaining `no-unused-vars` errors (15)** — found while auditing lint output; mostly trivial (unused imports/catch-block bindings), left as-is to avoid unrelated scope creep on other features:
  - `ai-service/src/__tests__/generator.test.ts:1` — unused `beforeEach` import
  - `recorder-service/src/__tests__/api.test.ts:1` — unused `afterAll` import
  - `recorder-service/src/recorder.ts:16` — unused `CapturedResponse` type
  - `results-service/src/__tests__/api.test.ts:739` — unused `workerService` variable
  - `results-service/src/app.ts:862` — unused `reply` handler param
  - `results-service/src/app.ts:958,976,1010,1028,1046,1081,1118,1138` — unused `err` in catch blocks (8 occurrences)
  - `results-service/src/consumer.ts:91` — unused `_` binding
  - `worker-backend/src/__tests__/index.test.ts:92` — unused `testId` param
  - `worker-client/src/retry.ts` — unused binding (reported alongside the above)
  - Also ~85 `@typescript-eslint/explicit-function-return-type` warnings across worker-backend/worker-client (parser.ts, index.ts, retry.ts, test files) — non-blocking, lower priority
- [x] **Flaky `teams.test.ts` under full-suite runs** — resolved by migrating all Testcontainers-backed integration tests to a single shared PostgreSQL container (`test-support/globalSetup.ts` + `sharedPostgres.ts`, commit `6f91bb7`), each test file creating its own isolated database on it instead of starting its own container. Full-suite run (`npm test`, 1297 tests) now passes `teams.test.ts` cleanly with no hook timeouts.
- [x] **Flaky perf-budget assertion in `parser.test.ts`** — `aggregateWindow handles a 2,000-line live window (5-10 groups) within budget` asserted `elapsed < 100ms`; failed at 137.9ms under full-suite CPU contention. Raised to 500ms to match the other 10k-line budget tests.
- [x] **noVNC auth in production** — `VNC_PASSWORD` env var passed to `x11vnc -passwd` in docker-entrypoint(.dev).sh (falls back to `-nopw` for local dev); docker-compose.prod.yml binds port 6080 to `127.0.0.1` only
- [x] **AMQP reconnect: replace bounded retry with unbounded backoff supervisor (G1 — chaos)** — `connectWithBackoff()` added to `@alt/shared` (capped exponential backoff, 1s → 2s → … → 30s cap, never gives up); all 5 queue-connected services (api-service, ai-service, worker-backend, worker-client, results-service) now use it for both the initial connect and reconnect-on-close, so the platform self-heals after a RabbitMQ outage of any length without operator intervention
- [x] **FlowBuilder: recording session lost on page reload/navigation** — `recording` state (session id/status) is now persisted to `localStorage` (`flowRecordingSession`) on every change; on mount, an in-progress (`active`/`stopping`) session is restored and `GET /recordings/:id` is called to resume polling, import steps if already completed, or clear stale state on 404/expiry
- [x] **results-service consumer.ts: missing AMQP close handler + false-positive health (G2 — chaos)** — `consumer.ts` now registers `connection.on('close'/'error')` and `channel.on('error')` handlers that flip `consumerConnected = false` and reconnect via the same `connectWithBackoff()` supervisor used by the other 4 services; `/health` and `/system/health` now reflect the real channel state during a RabbitMQ outage
- [x] **e2e templates.spec.ts: preset dropdown not visible after save+reload** — root cause: `page.getByRole('button', { name: /save.*preset/i }).click()` resolved coordinates that, during the description-driven re-render, landed on the "Advanced settings" toggle directly above the "Save preset" button — opening that section instead of clicking save, so `handleSavePreset` never ran and no `/presets` POST ever fired. Fixed by using `.dispatchEvent('click')` instead of coordinate-based `.click()`. Also relaxed the post-reload dropdown assertion to match AI-14's generated preset name (e.g. "HTTPBin Backend Baseline Load (backend)") case-insensitively. Also bounded `suggestPresetName()` in `handleSavePreset` (`services/ui/app/page.tsx`) with a 5s `Promise.race` timeout so a slow/unreachable Gemini call can't block the save.
- [x] **Run chaos testing with Rosetta** — `chaos/chaos.test.ts` (21 tests, Vitest) covers 8 fault categories: RabbitMQ DLQ retry exhaustion, PostgreSQL transient failure, AI provider unavailability + fallback chain, worker crash / stale cleanup (testcontainer), cancel race condition (testcontainer), concurrent quota enforcement, webhook delivery failure, WebSocket reconnect backoff. No new gaps found beyond G1–G5; all faults degrade gracefully. One observation: crashed-worker mean-time-to-recovery is bounded by `STALE_RUNNING_MINUTES` (15 min default).

### Medium Priority (Feature Gaps)

- [x] **Dark mode** — 🌙/☀ toggle in Sidebar; CSS vars override via `.dark` class; localStorage persistence; falls back to OS preference
- [x] **Multi-tenancy (full)** — org → teams → members hierarchy (`organizations`, `org_members`, `projects.org_id`); per-team API keys (`team_api_keys`, `/teams/:id/api-keys`); role-based access (admin/member/viewer) + org roles (owner/admin/member); team-scoped dashboards, results, scripts, schedules, webhooks, and presets; invite/remove members; team switcher in Sidebar; org admin panel (`/org` page)
- [x] **Quota management** — Per-team resource limits: max concurrent tests, max test duration, max VUs, max scheduled tests, Gemini API calls per day; quota tracking in PostgreSQL (`team_quotas` + `gemini_usage` tables); enforcement in api-service before queuing; quota usage dashboard in UI (`/team` page "Usage & Limits"); admin override via `PUT /teams/:id/quotas`
- [x] **k6 script library** — New "Library" page (`/library`) lists built-in k6 script templates (REST API load test, authenticated flow, file upload, GraphQL query, spike test) with tags + preview; "Use this script" loads the template into Custom Script mode on the home page via `?useScriptTemplate=<id>` (`services/ui/lib/scriptTemplates.ts`)
- [x] **Alerting integrations** — Format selector on webhooks: Generic JSON / Slack attachment / PagerDuty Events API v2 / OpsGenie Alert API; migration #4 adds `format` column
- [x] **HAR replay improvements** — Extended static-asset filter (adds mp4/mp3/pdf/map/xml/avif/cjs/mjs + MIME-type check); cap raised to 50; shows count of filtered entries
- [x] **Worker auto-scaling** — Investigated and documented in `docs/how-to/production.md` → "Horizontal scaling → Auto-scaling on queue depth": recommended approach is Kubernetes + KEDA's RabbitMQ scaler (`ScaledObject` per queue, `QueueLength` trigger on `backend-tests`/`client-tests` `messages_ready`), with a Docker Swarm polling-script alternative (`docker service scale` driven by the RabbitMQ management API); both rely on existing durable queues and workers' graceful SIGTERM drain
- [x] **Worker-offline signal on AI-routed path (G4 — chaos)** — `warnIfNoWorker()` is called after `tryPublish()` on every routing branch (custom script, cache miss/AI-routed, direct reuse, description comparison), checking `backend-tests`/`client-tests` consumer count regardless of whether the test was sent via `ai-requests` or directly to the worker queue; covered by `index.test.ts` "POST /tests — no-worker warning message"
- [x] **parseDurationSeconds accepts numeric values (G5 — chaos)** — `parseDurationSeconds` now accepts `string | number`; a bare number is treated as seconds directly (`Math.round`), so `duration: 10` no longer 500s or bypasses the duration quota check
- [x] **GET /results UI pagination** — Load more button using `nextBefore` cursor; appends to existing list; WebSocket updates still work on first page
- [x] **Download/upload ignore-pattern lists** — "↓ Export" / "↑ Import" buttons in FlowBuilder's "🚫 Ignore list" panel; export downloads `ignore-list.json` (JSON array of patterns) via `Blob`+`URL.createObjectURL`; import reads a JSON array of strings and merges it into `ignorePatterns` (de-duplicated via `Set`), alerting on invalid file format
- [x] **Show k6/Puppeteer execution logs on result detail page** — `execution_log TEXT` column (migration #12) on `test_results`; workers ANSI-strip and forward each log line via `POST /results/:testId/log-line` (internal, WS-broadcast) during execution, then include the full log in the RabbitMQ test-results message for persistence; `GET /results/:testId/log` (project-scoped); `ExecutionLogPanel` in `results/[testId]/page.tsx` — collapsible (collapsed by default), real-time streaming via `test:log` WS events, severity filter (ALL/INFO/WARN/ERROR/DEBUG with counts), copy button, download as `.txt` (client-side Blob), auto-scroll during live stream
- [x] **Live metrics result page freezes on long-running tests** — root cause was two of the three suspected causes at once: `GET /results/:testId/live` (used for the initial page load and every WS reconnect resync) had no cap and returned every stored row since the test started — for a long-running test this bypassed the 120-point rolling cap that only applied to incremental WS `test:live` appends, so the array (and the Recharts render cost) grew unboundedly with test duration; the "Test Timeline" view for a *completed* long test hit the same unbounded fetch. Fixed with `downsampleLivePoints()` (`results-service/src/liveMetricsDownsample.ts`) — uniformly sub-samples to `MAX_LIVE_POINTS` (300) once a test's row count exceeds it (always keeping the first/last row so the chart still spans the full run instead of truncating to a recent slice), applied in the `GET /live` handler. Also addressed the third suspected cause (rendering cost): `RealtimeChart` is now wrapped in `React.memo` so the result page's per-second elapsed/countdown timers no longer force a full Recharts re-render when `points`/`startedAt` haven't actually changed.
- [x] **Add error status codes to the live metric graph** — `aggregateWindow()` (`worker-backend/src/parser.ts`) now reads the `status` tag already present on each `http_reqs` Point in the k6 JSON stream (same data `parseK6All` uses for the post-test breakdown, previously discarded by the live path) and computes `clientErrorRate`/`serverErrorRate` (4xx/5xx as % of requests in the window) alongside the existing overall `errorRate`. Threaded through `LiveMetricPoint` (`@alt/shared`), the `POST`/`GET /results/:testId/live` routes + `live_metrics` table (migration #15 adds `client_error_rate`/`server_error_rate FLOAT DEFAULT 0` columns), and the WS `test:live` broadcast. `RealtimeChart.tsx`'s "VUs & Error Rate" panel adds two new lines (amber 4xx, dark red 5xx) alongside the existing total error-rate line — aggregate view only, per-step breakdown out of scope.

### Data Privacy Gaps

- [x] **PII detection/redaction before sending recorded traffic to Gemini** — `redactPII()`/`detectPII()` in `@alt/shared`; applied to `requestBody`/`responseBody` in `recorder-service/correlator.ts` before building the correlation prompt (emails, SSNs, phone numbers, Luhn-valid credit card numbers, IPv4 addresses)
- [x] **Encryption at rest for PostgreSQL** — Investigated `pgcrypto` column-level encryption vs. volume/disk-level encryption; documented in `docs/how-to/production.md` → "Encryption at rest". Recommendation: encrypt the `postgres_data` (and `rabbitmq_data`) volume/disk (cloud-managed DB encryption, encrypted cloud block storage, or LUKS/dm-crypt for bare metal) rather than per-column — `pgcrypto` would break indexed lookups on `target_url` and JSONB queries on `metrics`/`steps`/`analysis` used throughout results-service and the AI insight prompts
- [x] **Data retention / right-to-erasure (GDPR)** — `TEST_RESULTS_RETENTION_DAYS` env var (default `0` = disabled) auto-purges `test_results` (+ associated `live_metrics`) older than N days in `runStaleCleanup`; admin-only `DELETE /teams/:id/data` (body `{ confirm: true }`) erases all team data (test_results, live_metrics, test_scripts, schedules incl. cron unregistration, test_presets, webhooks, log_sources, audit_log) and records a final `erase_team_data` audit entry; "Danger Zone" section on Team page with two-click confirmation
- [x] **Audit log for data access** — Migration #9 adds `audit_log` (team_id, user_id, action, resource_type, resource_id, created_at); `recordAudit()` (`results-service/src/audit.ts`) logs `export_pdf`/`export_csv` on `/report.pdf`/`/report.csv` and `delete` on `DELETE /scripts/:id`; admin-only `GET /teams/:id/audit-log` returns the most recent entries joined with user email; surfaced in an "Audit Log" section on the Team page

### Low Priority (Polish)

- [x] **recorder-service image size** — Removed redundant `python3 py3-pip` + `pip3 install websockify`: the `novnc` apk package already pulls in `websockify` (and python3) as a transitive dependency, so the explicit pip install was duplicating ~15-20MB of py3-pip/setuptools/packaging. `Dockerfile`/`Dockerfile.dev` simplified to a single `apk add` of `chromium nss freetype harfbuzz ca-certificates ttf-freefont xvfb x11vnc novnc`; verified `docker compose build recorder-service` succeeds and entrypoint's `python3 -m websockify` still works via the apk-provided package
- [x] **Gemini rate limit UI** — `WorkerHealth.tsx` now polls `GET /teams/:id/quotas` every 60s (mirrors `AIStatus.tsx` polling) and shows a "Gemini X/Y today" chip in the persistent worker strip; turns red when usage hits the daily cap
- [x] **Test result export** — `GET /results/:testId/report.csv` returns summary metrics + a per-step section for flow tests; "↓ CSV" download button next to "↓ PDF" on the result detail page
- [x] **Custom k6 threshold validation** — `GET /results/preview-thresholds?url=&type=&thresholds=<json>` finds the latest completed result for that target URL+type and runs `analyzeResult(metrics, null, thresholds)` from `@alt/shared` to report `{ available, perfStatus, thresholdViolations, basedOn }`; "👁 Preview against last run" link in the SLO thresholds section of the home page shows pass/degraded/fail + violation details before the test is run
- [x] **Flow step ordering drag-and-drop** — Each step card in FlowBuilder is now `draggable` with a "⠿" drag handle (`onDragStart`/`onDragOver`/`onDrop`/`onDragEnd`, `moveStep(from, to)` splice-based reorder, highlight on the dragged card); ↑/↓ buttons retained alongside for accessibility/keyboard users

### Architecture Improvements

- [x] **Redis pub/sub for WebSocket** — `ws.ts` publishes every `broadcast()` event to a `ws:broadcast` Redis channel (when `REDIS_URL` set) and a duplicated subscriber connection forwards events from other replicas to its local `Set<WebSocket>`; an `INSTANCE_ID` tag prevents double-delivery on the originating replica; falls back to local-only broadcast when Redis is unconfigured
- [x] **Database read replica** — `readPool` in db.ts (falls back to primary when `READ_DATABASE_URL` unset); all heavy GET endpoints use `rPool`; `docker-compose.replica.yml` overlay for PostgreSQL streaming replication
- [x] **RabbitMQ persistence** — Audited all 5 queue-connected services: every queue (`ai-requests`, `backend-tests`, `client-tests`, `test-results`, and all `.dlq` queues) is asserted with `durable: true`, every `sendToQueue`/`publish` call uses `{ persistent: true }`, and `rabbitmq_data` is a named Docker volume (`docker-compose.yml`) so messages survive broker restarts. Only the `cancel-fanout` signal is non-persistent, which is correct — it targets ephemeral per-replica exclusive/auto-delete queues
- [x] **OpenTelemetry tracing** — `tracing.ts` in all 7 services; auto-instruments Fastify/pg/amqplib/fetch/k6; `test.id` span attribute; exports to Tempo via `OTEL_EXPORTER_OTLP_ENDPOINT`; disabled with `OTEL_SDK_DISABLED=true`
- [x] **worker-client TTFB accuracy** — Now uses `performance.getEntriesByType('navigation')[0].responseStart - requestStart` via `page.evaluate()` — excludes DNS/TCP/TLS/settle time

### MEGA Features (requires deep investigation)

- [x] **Natural language "one prompt" test creation (backend + browser)** — `POST /chat/parse` (results-service) + "Chat" tab in the UI; multi-turn conversation, Gemini infers type/URL/load options/thresholds, shows a read-only preview, runs via the existing `POST /tests` on confirm. See `docs/AI-FEATURES.md` → "13. Chat-Based Test Creation" and `plans/one-prompt-test-creation/` for the full spec/plan.
  - [x] **Follow-up — flow/multi-step inference (Phase 2, done 2026-06-28)** — chat now accepts `ChatAttachment[]` (Swagger/OpenAPI URLs, HAR recordings, documentation, codebase files); server fetches/parses each attachment server-side and injects context into the AI prompt. When context is present, a new `flowReady` outcome returns a complete `FlowTestConfig` (`FlowStep[]` + options) extracted from the spec/HAR. UI has file upload (paperclip), Swagger URL input, and drag-and-drop; `flowReady` renders a step list card with "Run flow test", "Edit in Flow Builder", and "Adjust settings" actions. `redirectToFlowBuilder` is now the last resort (only when multi-step intent is detected but no context is available). See `docs/AI-FEATURES.md` § 13 for full details.
